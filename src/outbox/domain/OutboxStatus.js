export const OutboxStatus = {
  PENDING: 'pending',
  PROCESSING: 'processing',
  PUBLISHED: 'published',
  FAILED: 'failed',
};

export const { PENDING, PROCESSING, PUBLISHED, FAILED } = OutboxStatus;
