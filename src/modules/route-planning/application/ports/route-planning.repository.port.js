/**
 * Port contract:
 *
 * createRoutePlanning(routePlanning)
 * updateRoutePlanning(routePlanning)
 * findRoutePlanningById(id)
 * listRoutePlannings(filters)
 * replacePlanningConditions(routePlanningId, conditions)
 * listPlanningConditions(routePlanningId)
 * savePlannedRoutes(routePlanningId, routes)
 * listPlannedRoutes(routePlanningId)
 * markPlannedRouteCreated(plannedRouteId, routeId)
 */
