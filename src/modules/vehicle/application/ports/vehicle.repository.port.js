/**
 * Port contract:
 *
 * findVehicleById(id)
 * findVehicleTypeById(id)
 * createVehicle(vehicle)
 * updateVehicle(vehicle)
 * updateVehicleLocation(vehicle)
 * listVehicles(filters)
 * createVehicleType(vehicleType)
 * listVehicleTypes(filters)
 */
