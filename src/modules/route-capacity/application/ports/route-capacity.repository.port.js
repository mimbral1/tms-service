/**
 * Port contract:
 *
 * createRouteCapacity(routeCapacity)
 * updateRouteCapacity(routeCapacity)
 * findRouteCapacityById(id)
 * listRouteCapacities(filters)
 * replaceWindows(routeCapacitySchemaId, windows)
 * listWindows(routeCapacitySchemaId)
 */
