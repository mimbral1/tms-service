/**
 * Port contract:
 *
 * createRoute(route)
 * updateRoute(route)
 * findRouteById(id)
 * findStopsByRouteId(routeId)
 * findActionsByStopId(routeStopId)
 * replaceStops(routeId, stops)
 * listRoutes(filters)
 * createRouteTracking(routeId, tracking, userId)
 * createVehicleTracking(routeId, point)
 */
